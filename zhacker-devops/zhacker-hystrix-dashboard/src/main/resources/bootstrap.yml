server:
  port: 9099
spring:
  application:
    name: zhacker-hystrix-dashboard
  cloud:
    config:
      label: master
      uri: http://localhost:9090
      name: ${spring.application.name:zhacker-hystrix-dashboard}
      profile: default
